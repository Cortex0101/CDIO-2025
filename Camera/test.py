
from GetBalls import get_ball_positions, cap, cv2
while True:
#  get_ball_positions.c2.imshow("edges", get_ball_positions.edges)
  
  print (get_ball_positions())
  #cv2.imshow("Edges", get_ball_positions().get("edges"))
  #cv2.imshow("Frame", get_ball_positions().get("frame"))